# Private Analytical Data

Place the institutionally approved, fully de-identified analytical export here as:

`pcoma_registry.csv`

This directory is ignored by Git. Do not place direct identifiers, reversible linkage keys, operator identifiers, or free-text clinical notes in the analytical export.
